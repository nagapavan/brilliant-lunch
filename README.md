# Brilliant Lunch Test

## Build:
Generates zip file for the dist directory.

```bash
npm install -g gulp
npm install
gulp build
```
## Local test:
Post build:
```bash
cd dist
npm run start
```

## Building docker image
```bash
docker-compose up
```